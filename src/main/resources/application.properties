spring.application.name=quant
spark.master=local[*]
# Spark UI disabled to avoid servlet dependency
spark.ui.enabled=false
# Optional: Enable UI later with port
# spark.ui.enabled=true
# spark.ui.port=4040
starting.capital = 100000
max.slots = 10

